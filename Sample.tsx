import React from "react";
import { StyleSheet, View } from "react-native";

const Com: React.FC = () => {
    return <>
        <View></View>
    </>
}

const styles = StyleSheet.create({

})

export default Com
